// Brand marks Remix Icon does not draw, copied verbatim from Simple Icons
// (CC0 1.0). One file per icon, `Si`-prefixed as Simple Icons' React package
// names them. Add one here only when Remix has no glyph for the brand.
export { SiSubstack } from './SiSubstack';
