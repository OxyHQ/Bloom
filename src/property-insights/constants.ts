/** `EnergyLabel`: with both columns, the marker tags carry their value from this width (below, the value sits in the column header). */
export const ENERGY_LABEL_INLINE_VALUES_MIN_WIDTH = 560;
/** `EnergyLabel`: one bar row's height. */
export const ENERGY_LABEL_ROW_HEIGHT = 24;
/** `EnergyLabel`: bar A's share of the scale column; each class adds 10%. */
export const ENERGY_LABEL_MIN_BAR_RATIO = 0.4;
/** `NeighbourhoodScores` (`columns="auto"`) draws two columns from this width. */
export const NEIGHBOURHOOD_SCORES_TWO_COLUMN_MIN_WIDTH = 560;
/** `PriceHistoryChart`'s plot height. */
export const PRICE_HISTORY_PLOT_HEIGHT = 200;
