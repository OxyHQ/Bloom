---
title: Listing Card
description: A home in a results grid — a stay, a rental, a sale or a swap — with paged photos, a heart, offerings, facts, price lines and status, a compact list row, the responsive grid, the heart on its own, and a saved-collection card.
order: 107
---

# Listing Card

```tsx
import {
  FavoriteButton,
  ListingCard,
  ListingCardGrid,
  WishlistCard,
} from '@oxy.so/bloom/listing-card';

<ListingCardGrid>
  {stays.map((stay) => (
    <ListingCard
      key={stay.id}
      photos={stay.photos}
      title="Alvora, Coast of Merin"
      subtitle="Hosted by Ilse"
      dates="12 – 17 Oct"
      rating={4.92}
      reviewCount={128}
      originalPrice="€150"
      price="€124"
      priceUnit="night"
      badge="Guest favourite"
      href={`/stays/${stay.id}`}
      onPress={() => router.push(`/stays/${stay.id}`)}
      favorite={saved.has(stay.id)}
      onFavoriteChange={(next) => setSaved(stay.id, next)}
    />
  ))}
</ListingCardGrid>
```

A home for rent and for sale, with its facts:

```tsx
import { RiBuilding2Line, RiDropLine, RiHotelBedLine, RiRulerLine } from '@oxy.so/bloom/icons';

<ListingCard
  photos={home.photos}
  title="Loft above the Ondel market"
  address="Plaça Ferran, Ondel"
  approximateLocation
  offerings={['long_term_rent', 'sale']}
  priceLines={[
    { price: '€1,250', unit: '/ month' },
    { price: '€310,000', secondary: '€4,130/m²' },
  ]}
  facts={[
    { icon: RiHotelBedLine, label: '2', accessibilityLabel: '2 bedrooms' },
    { icon: RiDropLine, label: '2', accessibilityLabel: '2 bathrooms' },
    { icon: RiRulerLine, label: '75 m²' },
    { icon: RiBuilding2Line, label: 'Floor 6' },
  ]}
  status="reserved"
  href={`/homes/${home.id}`}
/>
```

Prices, dates and counts arrive **pre-formatted** — the app owns currency, locale
and plural rules. Nothing here computes a price.

## `ListingCard`

| prop                                   | decides                                                                                                   |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `photos`                               | URLs or `ImageResolver` ids (resolved with `photoVariant`); the first is the cover                         |
| `title`                                | the headline, one line                                                                                     |
| `subtitle`, `dates`                    | two secondary lines, one line each                                                                         |
| `rating`, `reviewCount`, `newLabel`    | Bloom's `Rating`, right of the title. `undefined` draws nothing; `null` or `''` draws "New"                |
| `price`, `priceUnit`, `originalPrice`  | the price line: `originalPrice` struck through, `price` semibold, then the unit                           |
| `priceLines`                           | stacked price lines (`{ price, unit?, secondary?, originalPrice? }`); replaces the three props above, which are its single-line case |
| `total`                                | a line under the price lines; with none it IS the price line, semibold                                     |
| `address`, `approximateLocation`, `approximateLocationLabel` | a pin and the address line; `approximateLocation` adds "Approximate location" (or is the line) |
| `facts`                                | one row of `{ icon?, label, accessibilityLabel? }` — bed 3 · bath 2 · 110 m² · floor 4                    |
| `offerings`, `offeringLabels`          | `OfferingBadge`s — see below                                                                               |
| `status`, `statusLabel`                | `'available'` (default) or `'reserved'`, `'sold'`, `'rented'`, `'unavailable'` — see below                |
| `badge`                                | top-left over the photo — a string draws the white pill, any node is placed as given                      |
| `favorite`, `onFavoriteChange`         | the heart, top-right; drawn only when `onFavoriteChange` is given                                          |
| `onPress`, `href`                      | see below                                                                                                  |
| `loading`                              | a skeleton in the same geometry (`aria-busy`)                                                              |
| `layout`                               | `'vertical'` (default, photo 20:19) or `'horizontal'` (square photo 40% wide, for lists and map sheets)   |
| `density`                              | `'comfortable'` (default) or `'compact'`, a dense list row — see below                                    |
| `onPhotoIndexChange`                   | the photo in view changed                                                                                  |
| `accessibilityLabel`                   | replaces the composed name                                                                                 |
| `previousPhotoLabel`, `nextPhotoLabel`, `saveLabel`, `removeLabel` | the controls' names — translate them                                          |
| `style`, `testID`                      |                                                                                                            |

**Anatomy.** The photo is radius 16 and pages: swipe on touch, scroll on a
trackpad, and — on web, with a mouse — round previous/next buttons that fade in
while the pointer is over the card (or a button has keyboard focus). They are
absent on native and on touch-only browsers, where swiping is the gesture. Up to
five dots sit at the bottom; with more photos the five follow the one in view and
the edge dots shrink while more lie beyond. Photos mount as they come near, so a
card with twenty photos loads two.

**Press, and a real link.** With `href` the card is an `<a href>` on web, so
open-in-new-tab, copy link and middle-click work. With `onPress` as well, the
anchor's default navigation is prevented and `onPress` runs — a router takes it.
On native, `href` alone opens with `Linking`. With neither the card is not a
control.

**The controls are siblings of the link, not children.** The heart and arrows are
laid over the photo from outside the link: a button inside an anchor is invalid
HTML, and would make pressing the heart open the stay. Each is its own tab stop.

**Homes, not only stays.** The card draws every housing offering.

- **Price lines** stack one per line: an optional struck original, the price
  semibold, the unit, then `· secondary` in secondary text ("€240,000 ·
  €3,200/m²"). A home for rent and for sale passes two lines.
- **Facts** are body-2 in secondary text with 16px icons, 12 apart, on ONE row.
  The row wraps inside a box one line tall and clips, so facts that do not fit
  drop out whole from the end instead of each being cut to "11…". Give each a
  spoken `accessibilityLabel` ("3 bedrooms") — a bare "3" read aloud means nothing.
  Suggested icons: `RiHotelBedLine` (bedrooms), `RiDropLine` (bathrooms),
  `RiRulerLine` (area), `RiBuilding2Line` (floor).
- **Offerings go over the photo.** The top-left slot is one wrapping row, 6
  apart: the status pill, then `badge`, then the offerings as `onMedia`
  `OfferingBadge`s (duplicates drawn once). The photo is where a scanning eye
  lands, and keeping them off the text block keeps every card's text the same
  height, so a grid row stays aligned whatever each home is offered as. The slot
  stops 56 short of the right edge, clear of the heart.
- **Status.** Anything but `available` washes the photo 50% toward the page
  (`background`) and draws a pill first in the slot — the page's reading pair
  inverted (`text` fill, `background` label), legible in both modes. Labels:
  "Reserved", "Sold", "Rented", "Unavailable" (`statusLabel` translates).
- The **address line** (pin + body-regular secondary) sits under the subtitle;
  with `approximateLocation` it reads "Talmar Hill · Approximate location".

**Compact density.** `density="compact"` is a row for dense result lists: a
112 square thumbnail (radius 12, the first photo only — no paging, dots or
arrows) and, 12 to its right, the offerings and status as a row of small
**tinted** badges above the title (a 112 thumbnail has no room for pills over
it), the title with a small `Rating`, subtitle, address, dates, price lines and
facts at the body-2 size. The heart (20, a 32 target) sits over the thumbnail's
top-right corner, still a sibling of the link. `layout` is ignored; `loading`
draws a matching row skeleton.

**Accessible name.** The link is named for the stay — "Alvora, Coast of Merin,
Guest favourite, Rated 4.92 out of 5, 128 reviews, Hosted by Ilse, 12 – 17 Oct,
€124 night, originally €150". That sentence is English; apps in other languages
pass `accessibilityLabel`. A home adds, in order: the status after the title,
the offerings after the badge, the address after the subtitle, each price line
("€240,000, €3,200/m², originally €255,000") and the facts last.

## `FavoriteButton`

```tsx
<FavoriteButton favorite={saved} onFavoriteChange={setSaved} />
```

A heart for over a photo: a light outline around a dark body at 50%, legible on a
bright photo and a dark one; saved, the body turns the theme's red. Colour change
only — no scale. The target is at least 32 square (`size` sets the heart, default
24). It is a toggle — `aria-pressed` on web, `accessibilityState.selected` on native
— named for what pressing does: "Save to wishlist" / "Remove from wishlist"
(`saveLabel`, `removeLabel`). Its press does not reach a pressable card behind it.

## `ListingCardGrid`

```tsx
<ListingCardGrid>{cards}</ListingCardGrid>
<ListingCardGrid columns={2} columnGap={16} rowGap={24}>{cards}</ListingCardGrid>
```

Wraps its children into columns by its **own measured width** — 1 below 640, 2
below 950, 3 below 1280, 4 from there — so a grid beside a map counts the room it
has, not the window's. Gaps default to 24 between columns and 40 between rows.
`columns` fixes the count. Rendered as a list (`role="list"`, one `listitem` per
card).

Bloom's `Grid` (`Row`/`Col`) is not a fit here: it is one fixed row of fractional
columns that neither wraps nor responds to width.

## `WishlistCard`

```tsx
<WishlistCard name="Coast weekends" description="12 saved" photos={covers} href="/wishlists/coast" />
```

A saved collection: a square cover, radius 16, with 2px seams — one photo fills
it, two sit side by side, three put one tall photo beside two, four (or more) make
a 2×2 of the first four. The name and `description` sit under it. `onPress` and
`href` behave as on `ListingCard`; the name is "Coast weekends, 12 saved".

## Colours

All from the theme: the text is `text` / `textSecondary`; the badge pill and the
arrow buttons are the floating surface (the menu palette and Bloom's secondary
`Button`); the heart's outline and the dots are `neutral-50`, its body
`neutral-950` at 50%, saved `red-500` from the theme's `error` hue. The photo
placeholder is `neutral-100` (dark: the raised neutral). Offering badges take
their colours from `OfferingBadge` (`docs/offering-badge.mdx`).

## Disabled

The favourite button fades to `tokens.DISABLED_OPACITY` (0.5), not the 0.6 it
carried. See [Styles → Disabled](/docs/bloom/main/styles) for when a fade is
allowed at all.
